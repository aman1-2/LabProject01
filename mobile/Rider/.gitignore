
# Generated by `expo prebuild` — native projects are rebuilt from app.config.js,
# never edited by hand. Committing them means every prebuild shows thousands of
# changed files and the config stops being the source of truth.
/android/
/ios/
